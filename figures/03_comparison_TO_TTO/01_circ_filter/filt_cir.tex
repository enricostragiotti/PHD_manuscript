\documentclass[12pt, varwidth=4.5cm]{standalone}

\input{../../img_header}

\begin{document}

\begin{tikzpicture}
    \begin{axis}[
        general3D,
        width=6cm,
        height=6cm,
        view={135}{25}, % Adjust the view angle as needed
        % xlabel=$x$,
        % ylabel=$y$,
        zlabel=\footnotesize$w$,
		axis lines=center,
		axis on top,
		% every axis z label/.style={at={(ticklabel* cs:1)},anchor=south,},
    	% every axis y label/.style={at={(ticklabel* cs:1)},anchor=south,},
        % xmin=-1.5,
        % xmax=1.5,
        % ymin=-1.5,
        % ymax=1.5,
        % zmin=0,
        % zmax=1,
        ticks=none,
		% xticklabels={},
		% yticklabels={},
		% zticklabels={1},
		% extra z ticks={1},
		% extra z tick style={%
		% grid=major,
		% },
		% extra z tick labels={
		% 1,
		% },
        enlargelimits=0.3,
        % colormap={coolwarm}{rgb255(0cm)=(59,76,192); rgb255(1cm)=(180,4,38)}
    ]
    % Cone function with maximum at (0,0) and radius 1
    \addplot3[
        surf,
        shader=faceted,
		colormap name=coolwarm_r,
        domain=-1:1,
        domain y=-1:1,
        restrict z to domain*=0:1,
        samples=13, % Adjust the number of samples for smoothness
        ] {-sqrt(x^2 + y^2)+1};

	 % 2D circle on the XY plane
	 \addplot[
        thick,
        axis_gray,
        domain=0:360,
        samples=100,
    ] ({cos(x)}, {sin(x)});

	\draw[-stealth,axis_gray](0,0)--(-2^0.5/2,2^0.5/2) node[midway,axis_gray,below right=-4pt]{\footnotesize $R$};
    
    \end{axis}
\end{tikzpicture}

\end{document}